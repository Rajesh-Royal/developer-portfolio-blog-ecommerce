import React from "react";
import { Typography } from "@material-ui/core";

const ContactForm = () => {
    return (
        <div>
            <Typography variant="h1" color="initial">Contact form</Typography>
        </div>
    );
};

export default ContactForm;
